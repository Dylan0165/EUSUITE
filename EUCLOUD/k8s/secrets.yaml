# NOTE: This is a template. Replace with actual secrets before applying!
# Never commit real secrets to git!
apiVersion: v1
kind: Secret
metadata:
  name: eucloud-secrets
  namespace: eucloud
type: Opaque
stringData:
  secret-key: "CHANGE_THIS_TO_A_RANDOM_SECRET_KEY"
  jwt-secret-key: "CHANGE_THIS_TO_A_RANDOM_JWT_SECRET"
