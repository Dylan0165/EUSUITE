apiVersion: v1
kind: Service
metadata:
  name: eusuite-dashboard
  namespace: eucloud
spec:
  type: NodePort
  selector:
    app: eusuite-dashboard
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30091
