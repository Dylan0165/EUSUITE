apiVersion: v1
kind: Service
metadata:
  name: euadmin-frontend
  namespace: eucloud
  labels:
    app: euadmin-frontend
spec:
  type: NodePort
  selector:
    app: euadmin-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30096
      protocol: TCP
