apiVersion: apps/v1
kind: Deployment
metadata:
  name: eugroups-backend
  namespace: eucloud
  labels:
    app: eugroups-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eugroups-backend
  template:
    metadata:
      labels:
        app: eugroups-backend
    spec:
      imagePullSecrets:
        - name: docker-registry-secret
      containers:
        - name: eugroups-backend
          image: dylan016504/eugroups-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: eucloud-secrets
          env:
            - name: CORE_VALIDATE_URL
              value: "http://eucloud-backend/api/auth/validate"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
