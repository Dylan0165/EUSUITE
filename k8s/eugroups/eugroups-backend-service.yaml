apiVersion: v1
kind: Service
metadata:
  name: eugroups-backend
  namespace: eucloud
  labels:
    app: eugroups-backend
spec:
  type: NodePort
  selector:
    app: eugroups-backend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30600
      protocol: TCP
